[package]
name = "relativity"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/SuperInstance/relativity"
description = "Relativity in Rust — Lorentz transforms, spacetime metrics, geodesics, energy-momentum, cosmological models"

[dependencies]
serde = { version = "1", features = ["derive"] }
nalgebra = "0.33"
num-complex = "0.4"
